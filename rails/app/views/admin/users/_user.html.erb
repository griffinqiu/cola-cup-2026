<li id="admin_user_<%= user.id %>" class="<%= "gone" if user.deleted? %>">
  <span class="em"><%= user.emoji.presence || "👤" %></span>
  <span class="who">
    <span class="nm"><%= user.nickname %></span>
    <% if (handle = user.primary_handle).present? %>
      <span class="hd">@<%= handle %></span>
    <% end %>
  </span>
  <% if user == current_user %>
    <span class="self"><%= t(".self") %></span>
  <% elsif user.deleted? %>
    <%= button_to t(".restore"), restore_admin_user_path(user), method: :patch, class: "restore" %>
  <% else %>
    <%= button_to t(".delete"), admin_user_path(user), method: :delete, class: "delete" %>
  <% end %>
</li>
