<div class="pred-status">
  <% if available %>
    <p class="signed-lock">
      🔓 <%= t(".available_html") %>
    </p>
  <% else %>
    <p class="signed-lock">⏳ <%= t(".unavailable") %></p>
  <% end %>
</div>
