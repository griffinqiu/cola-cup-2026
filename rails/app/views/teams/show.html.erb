<% content_for :title, t(".page_title", team: @team.display_name) %>
<section>
  <div class="detail-head">
    <%= link_to root_path, class: "back-btn", data: { controller: "back", action: "click->back#go" } do %>
      <span class="bk-arrow">←</span> <%= t(".back") %>
    <% end %>
    <% group_match = @matches.find { |match| match.group_name.present? } %>
    <% if group_match && (letter = match_group_letter(group_match)) %>
      <%= link_to t(".group_standings_crumb", letter: letter), group_path(letter), class: "detail-crumb" %>
    <% end %>
  </div>
  <hr class="rule ink">

  <div class="team-hero">
    <span class="flag"><%= team_flag(@team) %></span>
    <h1 class="disp"><%= @team.display_name %></h1>
    <% if @team.name_zh.present? && @team.name_zh != @team.name %>
      <span class="sub"><%= @team.name %></span>
    <% end %>
  </div>

  <hr class="rule">
  <% if @matches.empty? %>
    <p class="schedule-empty" style="padding:36px 0;text-align:center;color:var(--low);font-size:13px;">
      <%= t(".no_matches") %>
    </p>
  <% else %>
    <%= render "matches/match_list", matches: @matches %>
  <% end %>
</section>
